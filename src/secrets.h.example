#pragma once

// Copy this file to `secrets.h` and fill in your values.
// `secrets.h` is gitignored; this template is committed.

#include <stdint.h>

// ---------- WiFi ----------
constexpr const char* WIFI_SSID     = "your-ssid";
constexpr const char* WIFI_PASSWORD = "your-wifi-password";

// ---------- MQTT broker (RabbitMQ MQTT plugin) ----------
constexpr const char* MQTT_HOST = "broker.example.com";
constexpr uint16_t    MQTT_PORT = 1883;          // 8883 for TLS
constexpr const char* MQTT_USER = "your-mqtt-user";
constexpr const char* MQTT_PASS = "your-mqtt-password";
